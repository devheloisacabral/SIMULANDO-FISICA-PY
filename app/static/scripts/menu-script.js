function toggleMenu() {
        var menu = document.getElementById("menu");
        if (menu.style.left === "100%") {
            menu.style.left = "90%";
        } else {
            menu.style.left = "100%";
        }
    }
